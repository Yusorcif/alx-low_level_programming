#include "main.h"
/**
 * _islower - function to lower case
 *
 * Return: 1 if it a lower case
 * and  0 otherwise
 */

int _islower(int c);
{
	if (c >= 97 && c <= 122)
		return (i);
	else
		return (0);
}
